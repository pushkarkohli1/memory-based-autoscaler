# abort script on any command that exit with a non zero value
set -e

cp memory_based_autoscaler/memory-based-autoscaler.zip ${BOSH_INSTALL_TARGET}
cp memory_based_autoscaler/manifest.yml ${BOSH_INSTALL_TARGET}
